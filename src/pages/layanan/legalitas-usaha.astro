---
import config from "@/config/config.json";
import Base from "@/layouts/Base.astro";
import PageHeader from "@/partials/PageHeader.astro";
import { markdownify } from "@/lib/utils/textConverter";
import { getEntry } from "astro:content";

const layanan = await getEntry("layanan", "legalitas-usaha");
const { Content } = await layanan.render();
const { title, description, meta_title, image } = layanan.data;

---

<Base 
  title={"Legalitas Usaha"}
  meta_title={meta_title}
  description={description}
  image={image}
>
  <PageHeader title={"Legalitas Usaha"} />
  <section class="section-sm">
    <div class="container">
      <div class="row justify-center">
        <div class="text-center md:col-10 lg:col-7">
          <h2 set:html={markdownify(title)} class="h3 mb-6" />
          <div class="content">
            <Content />
          </div>
        </div>
      </div>
    </div>
  </section>
</Base>
